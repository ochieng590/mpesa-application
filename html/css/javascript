<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>M-Pesa Payment Integration</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            width: 100%;
            max-width: 1000px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }
        
        header {
            background: #006400;
            color: white;
            padding: 20px;
            text-align: center;
        }
        
        .logo {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 10px;
        }
        
        .logo i {
            font-size: 32px;
            margin-right: 10px;
        }
        
        .logo h1 {
            font-size: 28px;
            font-weight: 700;
        }
        
        .content {
            display: flex;
            flex-wrap: wrap;
        }
        
        .payment-section {
            flex: 1;
            min-width: 300px;
            padding: 30px;
            background: #f9f9f9;
        }
        
        .history-section {
            flex: 1;
            min-width: 300px;
            padding: 30px;
            background: white;
            border-left: 1px solid #eee;
        }
        
        h2 {
            color: #006400;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #006400;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        
        input, select {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            transition: border 0.3s;
        }
        
        input:focus, select:focus {
            border-color: #006400;
            outline: none;
        }
        
        button {
            width: 100%;
            padding: 14px;
            background: #006400;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s;
        }
        
        button:hover {
            background: #008000;
        }
        
        .transaction {
            padding: 15px;
            margin-bottom: 15px;
            background: #f5f5f5;
            border-radius: 8px;
            border-left: 4px solid #006400;
        }
        
        .transaction-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
        }
        
        .amount {
            font-weight: 700;
            color: #006400;
        }
        
        .status {
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: 600;
        }
        
        .success {
            background: #e6f7ee;
            color: #009944;
        }
        
        .pending {
            background: #fef7ec;
            color: #ff9900;
        }
        
        .phone {
            color: #666;
            margin-bottom: 5px;
        }
        
        .date {
            color: #999;
            font-size: 12px;
        }
        
        .response {
            margin-top: 20px;
            padding: 15px;
            border-radius: 8px;
            background: #e6f7ee;
            color: #009944;
            display: none;
        }
        
        .footer {
            text-align: center;
            padding: 20px;
            color: #666;
            font-size: 14px;
            background: #f5f5f5;
            border-top: 1px solid #eee;
        }
        
        @media (max-width: 768px) {
            .content {
                flex-direction: column;
            }
            
            .history-section {
                border-left: none;
                border-top: 1px solid #eee;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">
                <i class="fas fa-money-bill-wave"></i>
                <h1>M-Pesa Payment Gateway</h1>
            </div>
            <p>Secure mobile money transactions</p>
        </header>
        
        <div class="content">
            <div class="payment-section">
                <h2>Make Payment</h2>
                <form id="payment-form">
                    <div class="form-group">
                        <label for="phone">Phone Number</label>
                        <input type="text" id="phone" placeholder="e.g. 254712345678" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="amount">Amount (KSh)</label>
                        <input type="number" id="amount" placeholder="Enter amount" min="10" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="account">Account Reference</label>
                        <input type="text" id="account" placeholder="e.g. Invoice #123" required>
                    </div>
                    
                    <button type="submit">Initiate Payment <i class="fas fa-arrow-right"></i></button>
                </form>
                
                <div class="response" id="response">
                    <i class="fas fa-check-circle"></i> 
                    <span id="response-text"></span>
                </div>
            </div>
            
            <div class="history-section">
                <h2>Recent Transactions</h2>
                
                <div class="transaction">
                    <div class="transaction-header">
                        <span class="amount">KSh 1,250</span>
                        <span class="status success">Completed</span>
                    </div>
                    <div class="phone">+254 711875310</div>
                    <div class="date">Today, 10:23 AM</div>
                </div>
                
                <div class="transaction">
                    <div class="transaction-header">
                        <span class="amount">KSh 5,000</span>
                        <span class="status success">Completed</span>
                    </div>
                    <div class="phone">+254 711875310</div>
                    <div class="date">Yesterday, 3:45 PM</div>
                </div>
                
                <div class="transaction">
                    <div class="transaction-header">
                        <span class="amount">KSh 2,800</span>
                        <span class="status pending">Pending</span>
                    </div>
                    <div class="phone">+254 711875310</div>
                    <div class="date">Today, 11:15 AM</div>
                </div>
                
                <div class="transaction">
                    <div class="transaction-header">
                        <span class="amount">KSh 3,450</span>
                        <span class="status success">Completed</span>
                    </div>
                    <div class="phone">+254 745 678 901</div>
                    <div class="date">23 Oct 2023, 9:30 AM</div>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>© 2025 M-Pesa Integration Demo. .</p>
            <p>Secure payments with Safaricom M-Pesa</p>
        </div>
    </div>

    <script>
        document.getElementById('payment-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const phone = document.getElementById('phone').value;
            const amount = document.getElementById('amount').value;
            const account = document.getElementById('account').value;
            
            // Simulate API call to M-Pesa
            simulateMpesaPayment(phone, amount, account);
        });
        
        function simulateMpesaPayment(phone, amount, account) {
            // Show loading state
            const button = document.querySelector('button');
            const originalText = button.innerHTML;
            button.innerHTML = 'Processing... <i class="fas fa-spinner fa-spin"></i>';
            button.disabled = true;
            
            // Simulate API delay
            setTimeout(() => {
                // Show success response
                const response = document.getElementById('response');
                const responseText = document.getElementById('response-text');
                
                response.style.display = 'block';
                responseText.textContent = `Payment request of KSh ${amount} sent to ${phone}. Please check your phone to complete the transaction.`;
                
                // Reset button
                button.innerHTML = originalText;
                button.disabled = false;
                
                // Clear form
                document.getElementById('payment-form').reset();
                
                // Add to transaction history (in a real app, this would come from the backend)
                addTransactionToHistory(phone, amount, account);
            }, 2000);
        }
        
        function addTransactionToHistory(phone, amount, account) {
            const historySection = document.querySelector('.history-section');
            const transaction = document.createElement('div');
            transaction.className = 'transaction';
            
            const formattedAmount = 'KSh ' + Number(amount).toLocaleString();
            const now = new Date();
            const options = { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' };
            const dateString = now.toLocaleDateString('en-US', options);
            
            transaction.innerHTML = `
                <div class="transaction-header">
                    <span class="amount">${formattedAmount}</span>
                    <span class="status pending">Pending</span>
                </div>
                <div class="phone">${phone}</div>
                <div class="date">${dateString}</div>
            `;
            
            historySection.insertBefore(transaction, historySection.childNodes[2]);
        }
    </script>
</body>
</html>
